# Processed Knowledge Artifacts

This local folder is for processed text, figure evidence, table extraction,
embeddings, chunks, and intermediate fusion data derived from web research or
user references.

Most generated content here is ignored by Git. Keep only reusable templates or
small metadata files under version control.
